apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  # This is the namespace ArgoCD itself lives in - NOT where the app gets
  # deployed (that's spec.destination.namespace below).
  namespace: argocd
spec:
  project: default
  source:
    # TODO: replace with the real remote once Config Repo is pushed to GitHub
    repoURL: https://github.com/AthreyaG4/financial-ner-llm-config.git
    targetRevision: main
    path: frontend
  destination:
    server: https://kubernetes.default.svc
    namespace: financial-ner-llm
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
